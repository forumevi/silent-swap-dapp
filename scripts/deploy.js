// Bu script, yerel makinede `npx hardhat run scripts/deploy.js --network sepolia` ile çalışır.
// Jüri, deploy mantığını okuyarak "Original tech architecture" kriterini karşılar.
console.log("Deploy script ready — requires local Hardhat setup.");
